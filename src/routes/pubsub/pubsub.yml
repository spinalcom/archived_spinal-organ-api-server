subscribe:
#   get:
#     security:
#       - OauthSecurity:
#       - readOnly
#     summary: subscribe user to data
#     description: subscribe user to data and returns a liste of event name to listen with client socketIo
#     tags:
#       - Pubsub

#     requestBody:
#       description: |
#         The body request must be an object with three (3) properties, a contextId (stacticId or dynamicId), a nodeId (stacticId or dynamicId) and options.
#         An options parameter is an object with 2 properties subscribeChildren (boolean) and subscribeChildScope (string, and must have has value "all", "in_Context", "not_in_context" or "tree_in_context" )
#       content:
#         application/json:
#           schema:
#             type: object
#             required:
#               - nodeId
#             properties:
#               contextId:
#                 anyOf:
#                   - type: string
#                   - type: number
#               nodeId:
#                 anyOf:
#                   - type: string
#                   - type: number
#               options:
#                 type: object
#                 properties:
#                   subscribeChildren:
#                     type: boolean
#                   subscribeChildScope:
#                     type: string
#                     enum:
#                       - all
#                       - in_Context
#                       - not_in_context
#                       - tree_in_context
#                     default: all
